const userErrors = {
  emailError: 'Invalid e-mail format',
  passwordError: 'Not allowed password',
  nameError: 'Invalid name format',
  requiredError: 'All fields are required',
};

export default userErrors;
